# knaovim
> My neovim config to keep updated through its usage.

## Dependencies
1) Patched Terminal Font (i.e. Nerd Font)
2) git
3) fzf
4) ripgrep
5) tree-sitter-cli

> Make sure you run :Mason and install the enabled lsp's
